#if문과 else문을 쓰면 상황이 두가지.
#elif를 추가하면 더 많은 상황이 생김

# if와 else로도 구현이 가능하지만 가독성이 좋지 않음

#if 온도가 10도 이하이다:
#    자켓을 입는다
#else:
#    if 온도가 15도 이하다:
#        긴팔을 입는다
#    else:
#        반팔을 입는다

# elif문으로 else바로 밑에 오는 if문을 조건식으로 1줄로 변경
#if 온도가 15도 이상이다:
#    반팔을 입는다
#elif 온도가 10도 이상이다:
#    긴팔을 입는다
#else:
#    자켓을 입는다.


